info:
  name: search-results
  type: http

http:
  method: GET
  url: http://127.0.0.1:8000/search-results?coveredMinPrice=300000
  params:
    - name: coveredMinPrice
      value: "300000"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
